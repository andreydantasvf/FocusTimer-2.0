const buttonPlay = document.querySelector('button.play')
const buttonPause = document.querySelector('button.pause')
const buttonStop = document.querySelector('button.stop')
const buttonSet = document.querySelector('button.set')
const buttonIncrease = document.querySelector('button.increase')
const buttonDecrease = document.querySelector('button.decrease')

const buttonSoundForest = document.querySelector('button#forest-sound')
const buttonSoundRain = document.querySelector('button#rain-sound')
const buttonSoundCoffeeShop = document.querySelector('button#coffeeShop-sound')
const buttonSoundFireplace = document.querySelector('button#fireplace-sound')

const buttonLightMode = document.querySelector('button.light-mode')
const buttonDarkMode = document.querySelector('button.dark-mode')

const minutesDisplay = document.querySelector('.minutes')
const secondsDisplay = document.querySelector('.seconds')

export {
    buttonPlay,
    buttonPause,
    buttonStop,
    buttonSet,
    buttonIncrease,
    buttonDecrease,
    minutesDisplay,
    secondsDisplay,
    buttonSoundForest,
    buttonSoundRain,
    buttonSoundCoffeeShop,
    buttonSoundFireplace,
    buttonLightMode,
    buttonDarkMode
}